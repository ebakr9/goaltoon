# API-Football v3 — https://www.api-football.com
# Free plan: 100 req/day. Redis caching is critical to stay within limits.
API_FOOTBALL_KEY=

# Upstash Redis (strongly recommended — prevents repeat API calls across users)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Supabase (optional — not actively used yet)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
